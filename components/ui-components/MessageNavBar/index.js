import styles from './MessageNavBar.module.css'

const MessageNavBar = () => {
    return (
        <div className={styles.profileNavbar}>
            <div className={styles.profileTitle}>
                Message
            </div>
            <div></div>
        </div>
    );
};

export default MessageNavBar;